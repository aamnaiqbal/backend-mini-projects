<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
  </head>
  <body>
    <div class="w-full min-h-screen border-gray-900 p-10">
      <a href="/allUsers" class="text-blue-500 mb-10 italic hover:underline"
        >Go Back</a
      >
      <h2 class="text-2xl font-semibold mb-4">Update User</h2>
      <form
        action="/update/<%= user._id %>"
        method="POST"
        class="flex flex-col gap-4 w-1/3"
      >
        <input
          type="text"
          name="name"
          placeholder="name"
          class="border border-gray-300 p-2 rounded-md"
          value="<%= user.name %>"
        />
        <input
          type="text"
          name="email"
          placeholder="email"
          class="border border-gray-300 p-2 rounded-md"
          value="<%= user.email %>"
        />
        <input
          type="text"
          name="imageUrl"
          placeholder="image url"
          class="border border-gray-300 p-2 rounded-md"
          value="<%= user.imageUrl %>"
        />
        <button type="submit" class="bg-blue-500 text-white p-2 rounded-md">
          Update
        </button>
      </form>
    </div>
  </body>
</html>
